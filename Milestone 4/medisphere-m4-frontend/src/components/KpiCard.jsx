export default function KpiCard({label,value,trend,trendDirection='up',icon}){const arrow=trendDirection==='up'?'↑':trendDirection==='down'?'↓':'•';return <div className="kpi-card"><div className="kpi-top"><div className="kpi-label">{label}</div>{icon&&<span className="kpi-icon"><i className={`bi bi-${icon}`}/></span>}</div><div className="kpi-value">{value}</div><div className={`kpi-trend trend-${trendDirection}`}>{arrow} {trend}</div></div>}
