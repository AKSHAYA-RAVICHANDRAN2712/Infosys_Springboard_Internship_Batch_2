# Not needed in normal use -- the app talks to /api by default, which
# works automatically in both dev mode (via the Vite proxy) and
# production mode (served by the backend, same origin).
# Only set this if you're running the backend on a different host/port.
# VITE_API_URL=http://localhost:4001/api
