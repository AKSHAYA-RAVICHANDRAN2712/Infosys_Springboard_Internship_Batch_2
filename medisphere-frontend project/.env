# Base URL of the Java (Spring Boot) backend REST API
VITE_API_BASE_URL=http://localhost:8080/api

# Set to "true" to run the UI against built-in mock data instead of the real backend
VITE_USE_MOCK_DATA=true
